<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <a asp-action="Create" class="btn btn-success">Add New Task</a>
        @{
            int index = 1;
        }

        @foreach (var task in Model)
        {
            <tr>
                <td>@index</td>
                <td>@task.Title</td>
                <td>@task.Title</td>
                <td>@task.Description</td>
                <td>
                    @if (task.IsCompleted)
                    {
                        <span class="text-success">Completed</span>
                    }
                    else
                    {
                        <span class="text-danger">Not Completed</span>
                    }
                </td>
                <td>
                    <!-- Тук ще добавиш бутона за променяне на статуса -->
                    <form asp-action="ToggleStatus" asp-route-id="@task.Id" method="post">
                        <button type="submit" class="btn btn-primary">
                            @(task.IsCompleted ? "Mark as Incomplete" : "Mark as Complete")
                        </button>
                    </form>
                    <a asp-action="Delete" asp-route-id="@task.Id" class="btn btn-danger">Delete</a>

                    <a asp-action="Edit" asp-route-id="@task.Id" class="btn btn-warning">Edit</a>
                </td>

            </tr>
            {
                index++;
            }
        }
    </tbody>
</table>